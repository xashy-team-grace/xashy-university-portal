# File: .github/workflows/build.yml

name: Build and Upload WAR Artifact

on:
  push:
    branches:
      - fomo-actions  # Trigger only on push to 'fomo-actions' branch

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the code
      - name: Checkout code
        uses: actions/checkout@v4

      # Step 2: Set up JDK 11
      - name: Set up JDK 11
        uses: actions/setup-java@v4
        with:
          java-version: '11'
          distribution: 'temurin'
          cache: maven

      # Step 3 (Recommended): Cache Maven dependencies
      - name: Cache Maven packages
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      # Step 4: Build the project with Maven
      - name: Build with Maven
        run: mvn -B package --file pom.xml

      # Step 5: Security Scan with SonarQube
      - name: Security Scan with SonarQube
        uses: SonarSource/sonarqube-scan-action@v8
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
        with:
          projectKey: fomo-app

      # Step 6: Upload the resulting WAR as a workflow artifact
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: fomo-app-build
          path: target/*.war
          if-no-files-found: error
       

